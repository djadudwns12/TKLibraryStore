<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.tn.mapper.cartMapper">

	<insert id="putCart">
		insert into Cart(bookNo, qty, userId)
		values(#{bookNo}, #{qty}, #{userId})
	</insert>

	<!-- 특정 회원의 카트 리스트를 가져오는 쿼리문 -->
	<select id="getCartList" resultType="com.tn.cart.model.dto.CartDTO">
		SELECT b.thumbNail, b.title, b.price, b.salePrice, c.cartId, c.qty
		FROM Cart c
		INNER JOIN BookList b ON c.bookNo = b.bookNo
		WHERE c.userId = #{userId};
	</select>
</mapper>